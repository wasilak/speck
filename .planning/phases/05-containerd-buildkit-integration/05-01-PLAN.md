---
phase: 05-containerd-buildkit-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/speck-vz/src/config.rs
  - crates/speck-vz/src/error.rs
  - crates/speck-vz/Cargo.toml
autonomous: true
requirements: [RUN-06]
user_setup: []

must_haves:
  truths:
    - "GuestConfig has 5 new optional fields: rootfs_disk_path, data_disk_path, containerd_vsock_port, buildkitd_vsock_port, ready_vsock_port"
    - "GuestConfig::validate() rejects set but non-existent rootfs_disk_path and data_disk_path"
    - "GuestConfigBuilder has builder methods for all 5 new fields following the dns_vsock_port pattern"
    - "Error enum has DiskAttachment(String) and GuestReadyTimeout variants"
    - "speck-vz/Cargo.toml enables VZVirtioBlockDeviceConfiguration, VZDiskImageStorageDeviceAttachment, VZStorageDeviceConfiguration, VZStorageDeviceAttachment features on objc2-virtualization"
    - "cargo build -p speck-vz passes after changes"
  artifacts:
    - path: "crates/speck-vz/src/config.rs"
      provides: "GuestConfig + GuestConfigBuilder with 5 new optional fields"
      contains: "rootfs_disk_path"
    - path: "crates/speck-vz/src/error.rs"
      provides: "Error enum with DiskAttachment and GuestReadyTimeout variants"
      contains: "GuestReadyTimeout"
    - path: "crates/speck-vz/Cargo.toml"
      provides: "objc2-virtualization feature flags for virtio-blk disk attachment"
      contains: "VZVirtioBlockDeviceConfiguration"
  key_links:
    - from: "crates/speck-vz/src/config.rs"
      to: "validate()"
      via: "path.exists() check for rootfs_disk_path and data_disk_path"
      pattern: "rootfs_disk_path.*exists"
    - from: "crates/speck-vz/Cargo.toml"
      to: "objc2-virtualization"
      via: "features array"
      pattern: "VZVirtioBlockDeviceConfiguration"
---

<objective>
Add the type-level foundation for Phase 5: five new optional fields on `GuestConfig` (disk paths + vsock ports for containerd/buildkitd/READY), two new `Error` variants for disk and ready-signal failures, and four new `objc2-virtualization` feature flags needed to attach virtio-blk block devices.

Purpose: Plan 04 (disk attachment in `do_start`) and Plan 05 (Guest API) depend on these type definitions. This plan has no runtime behavior — it only establishes types and Cargo features.

Output: Updated `config.rs` (5 new fields + builder + validation), updated `error.rs` (2 new variants), updated `speck-vz/Cargo.toml` (4 new objc2-vz feature flags).
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-containerd-buildkit-integration/05-CONTEXT.md
@.planning/phases/05-containerd-buildkit-integration/05-RESEARCH.md
@.planning/phases/05-containerd-buildkit-integration/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
<name>Task 1: Extend GuestConfig + GuestConfigBuilder with Phase 5 fields</name>
<files>crates/speck-vz/src/config.rs</files>
<read_first>
  - crates/speck-vz/src/config.rs (read in full — understand existing struct, Default, Builder, validate() before modifying)
</read_first>
<behavior>
  - GuestConfigBuilder::rootfs_disk_path(p: impl Into&lt;PathBuf&gt;) sets rootfs_disk_path: Some(p.into())
  - GuestConfigBuilder::build() propagates rootfs_disk_path, data_disk_path, containerd_vsock_port, buildkitd_vsock_port, ready_vsock_port to GuestConfig
  - GuestConfig::validate() returns Err when rootfs_disk_path is Some but file does not exist
  - GuestConfig::validate() returns Err when data_disk_path is Some but file does not exist
  - GuestConfig::validate() returns Ok when both disk paths are None (fields are optional)
</behavior>
<action>
Add five new fields to the `GuestConfig` struct (after `dns_vsock_port`), following the exact field documentation style used for that field:

  pub rootfs_disk_path: Option&lt;PathBuf&gt;  — path to ext4 rootfs disk image (/dev/vda in guest)
  pub data_disk_path: Option&lt;PathBuf&gt;    — path to ext4 data disk image (/dev/vdb, /var/lib/containerd)
  pub containerd_vsock_port: Option&lt;u32&gt; — vsock port for containerd forwarder (recommended: 9001)
  pub buildkitd_vsock_port: Option&lt;u32&gt;  — vsock port for buildkitd forwarder (recommended: 9002)
  pub ready_vsock_port: Option&lt;u32&gt;      — vsock port for vminitd READY signal (recommended: 9000)

Add all five fields to `Default` impl (all None).

Add identical fields to `GuestConfigBuilder` struct and `Default` impl (all None).

Add five builder setter methods to `GuestConfigBuilder` impl block, following the `dns_vsock_port` setter pattern:
  - `pub fn rootfs_disk_path(mut self, path: impl Into&lt;PathBuf&gt;) -> Self`
  - `pub fn data_disk_path(mut self, path: impl Into&lt;PathBuf&gt;) -> Self`
  - `pub fn containerd_vsock_port(mut self, port: u32) -> Self`
  - `pub fn buildkitd_vsock_port(mut self, port: u32) -> Self`
  - `pub fn ready_vsock_port(mut self, port: u32) -> Self`

Update `GuestConfigBuilder::build()` to transfer all five new fields to the constructed `GuestConfig`.

Extend `GuestConfig::validate()` with existence checks for the two PathBuf fields, following the `initrd_path` pattern — use `if let Some(ref path) = self.rootfs_disk_path && !path.exists()` for each disk field.

Add a unit test `test_guest_config_phase5_fields` in the existing `#[cfg(test)]` block (or create one) that:
1. Builds a GuestConfig using the builder with all 5 new fields set to dummy values
2. Asserts all 5 fields propagate correctly from builder to struct
3. Asserts `validate()` on a valid kernel_path + non-existent rootfs_disk_path returns Err containing "rootfs"
</action>
<verify>
  <automated>cd /Users/piotrek/git/speck && cargo test -p speck-vz --lib -- config 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria>
  - `cargo test -p speck-vz --lib -- config` passes with test_guest_config_phase5_fields green
  - `crates/speck-vz/src/config.rs` contains `pub rootfs_disk_path: Option&lt;PathBuf&gt;`
  - `crates/speck-vz/src/config.rs` contains `pub ready_vsock_port: Option&lt;u32&gt;`
  - `crates/speck-vz/src/config.rs` contains `pub fn rootfs_disk_path(mut self, path: impl Into&lt;PathBuf&gt;) -> Self`
  - `crates/speck-vz/src/config.rs` validate() block contains a check on `rootfs_disk_path` with `.exists()`
  - `crates/speck-vz/src/config.rs` validate() block contains a check on `data_disk_path` with `.exists()`
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: Add error variants + objc2-virtualization feature flags</name>
<files>crates/speck-vz/src/error.rs, crates/speck-vz/Cargo.toml</files>
<read_first>
  - crates/speck-vz/src/error.rs (read in full — understand existing Error enum before adding variants)
  - crates/speck-vz/Cargo.toml (read in full — understand existing objc2-virtualization features list before extending)
</read_first>
<action>
In `crates/speck-vz/src/error.rs`, add two new variants to the `Error` enum after `NetworkIo`:

  #[error("Disk attachment error: {0}")]
  DiskAttachment(String)

  #[error("Guest ready signal timed out")]
  GuestReadyTimeout

Do not add other variants; do not modify existing variants.

In `crates/speck-vz/Cargo.toml`, extend the `objc2-virtualization` dependency's `features` array with these four entries (append after the current last feature entry):
  "VZStorageDeviceConfiguration"
  "VZVirtioBlockDeviceConfiguration"
  "VZStorageDeviceAttachment"
  "VZDiskImageStorageDeviceAttachment"

Do not modify any other sections of Cargo.toml. Do not add new dependencies.
</action>
<verify>
  <automated>cd /Users/piotrek/git/speck && cargo build -p speck-vz 2>&1 | tail -20</automated>
</verify>
<acceptance_criteria>
  - `cargo build -p speck-vz` succeeds
  - `crates/speck-vz/src/error.rs` contains `DiskAttachment(String)` variant
  - `crates/speck-vz/src/error.rs` contains `GuestReadyTimeout` variant
  - `crates/speck-vz/Cargo.toml` contains `"VZVirtioBlockDeviceConfiguration"` in the objc2-virtualization features list
  - `crates/speck-vz/Cargo.toml` contains `"VZDiskImageStorageDeviceAttachment"` in the objc2-virtualization features list
  - `cargo clippy -p speck-vz -- -Dwarnings` passes (no unused variant warnings — variants will be used in Plan 04)
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan contains only type definitions, error variants, and Cargo feature flags. No runtime trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | GuestConfig disk path fields | mitigate | validate() checks disk paths exist before VM start — rejects paths to non-existent files at config validation time, not at attach time |
| T-05-SC | Tampering | objc2-virtualization new feature flags | accept | Features are sub-feature gates on an existing vetted dependency (0.3.2 already in Cargo.lock); no new package is fetched |
</threat_model>

<verification>
- `cargo build -p speck-vz` passes with new features enabled
- `cargo test -p speck-vz --lib -- config` passes including new test
- `cargo clippy -p speck-vz -- -Dwarnings` clean
</verification>

<success_criteria>
- `GuestConfig` struct has all 5 new optional fields with builder methods and Default impls
- `GuestConfig::validate()` rejects set-but-missing disk paths
- `Error` enum has `DiskAttachment` and `GuestReadyTimeout` variants
- `objc2-virtualization` features include the 4 virtio-blk storage classes
- All existing speck-vz tests still pass
</success_criteria>

## Artifacts This Phase Produces

**New struct fields (GuestConfig):**
- `rootfs_disk_path: Option<PathBuf>` — path to ext4 rootfs image
- `data_disk_path: Option<PathBuf>` — path to ext4 data disk image
- `containerd_vsock_port: Option<u32>` — vsock port for containerd forwarder
- `buildkitd_vsock_port: Option<u32>` — vsock port for buildkitd forwarder
- `ready_vsock_port: Option<u32>` — vsock port for READY signal

**New builder methods (GuestConfigBuilder):**
- `rootfs_disk_path(path: impl Into<PathBuf>) -> Self`
- `data_disk_path(path: impl Into<PathBuf>) -> Self`
- `containerd_vsock_port(port: u32) -> Self`
- `buildkitd_vsock_port(port: u32) -> Self`
- `ready_vsock_port(port: u32) -> Self`

**New error variants:**
- `Error::DiskAttachment(String)` — virtio-blk attachment failure
- `Error::GuestReadyTimeout` — vminitd READY signal not received in time

<output>
Create `.planning/phases/05-containerd-buildkit-integration/05-01-SUMMARY.md` when done.
</output>
