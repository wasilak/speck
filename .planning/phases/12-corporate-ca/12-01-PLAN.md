---
phase: 12-corporate-ca
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - crates/speck-cli/src/config.rs
  - crates/speck-vz/src/config.rs
  - crates/speck-vz/src/virtiofs.rs
autonomous: true
requirements:
  - CERT-01
  - CERT-02
  - CERT-03
  - CERT-04
must_haves:
  truths:
    - "Test suite catches missing CA cert files before VM starts"
    - "Test suite catches invalid PEM files before VM starts"
    - "Test suite validates correct PEM files pass validation"
    - "Test suite confirms GuestConfig propagates ca_certs_paths from builder"
    - "Test suite confirms ca_certs_tag appears in kernel cmdline"
  artifacts:
    - path: "crates/speck-cli/src/config.rs"
      provides: "validate_and_prepare_ca_certs test cases"
      min_lines: 80
    - path: "crates/speck-vz/src/config.rs"
      provides: "GuestConfig ca_certs_paths builder propagation test"
      min_lines: 20
    - path: "crates/speck-vz/src/virtiofs.rs"
      provides: "ca_certs_tag in cmdline output test"
      min_lines: 20
  key_links:
    - from: "config_tests"
      to: "validate_and_prepare_ca_certs"
      pattern: "validate_and_prepare_ca_certs"
    - from: "config_tests"
      to: "CaConfig"
      pattern: "CaConfig"
---

<objective>
Write failing tests for the Corporate CA Injection features before any implementation code. These tests drive the RED→GREEN cycle for Plan 02 and Plan 03.

Purpose: Lock in observable behavior contracts (CERT-01 through CERT-04) as executable specifications before touching production code.
Output: Three test groups — validation tests in config.rs, guest config propagation tests, virtiofs cmdline tests.
</objective>

<execution_context>
@.planning/phases/12-corporate-ca/12-CONTEXT.md
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/piotrek/git/speck/crates/speck-cli/src/config.rs
@/Users/piotrek/git/speck/crates/speck-vz/src/config.rs
@/Users/piotrek/git/speck/crates/speck-vz/src/virtiofs.rs

The PEM test fixtures will be created inline: a valid cert generated at test time, or a canned PEM string. No external test data files needed.
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Test validate_and_prepare_ca_certs contract</name>
  <files>crates/speck-cli/src/config.rs</files>
  <read_first>
    @/Users/piotrek/git/speck/crates/speck-cli/src/config.rs (existing test module at bottom of file — lines 440+)
  </read_first>
  <behavior>
    - Test 1: Empty cert list returns None (no paths → skip CA setup)
    - Test 2: Missing file path returns Err with "file not found" (CERT-04)
    - Test 3: File with non-PEM content returns Err with "not valid PEM" (CERT-04)
    - Test 4: Valid PEM file returns Ok with dedup'd filename
    - Test 5: Two identical certs deduplicate (same SHA256 → only one file copied)
    - Test 6: Output paths are under "{speck_home}/ca-certs/" directory
  </behavior>
  <action>
    Add a `#[cfg(test)] mod tests` block (or extend the existing one) at the bottom of config.rs with tests for `validate_and_prepare_ca_certs`.

    - Use `std::env::temp_dir()` for test scratch directories.
    - Create test PEM files inline: a `b"-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----\n"` byte string.
    - For non-PEM content: write `b"not a certificate"`.
    - For duplicate detection: two paths pointing to the same file content.
    - For missing file: `PathBuf::from("/nonexistent/ca.pem")`.

    Use `anyhow::Result` return type on test fns so `?` works. Clean up scratch dirs in each test.

    IMPORTANT: These tests MUST fail on first run because `validate_and_prepare_ca_certs` does not exist yet (RED). That is expected and correct.
  </action>
  <verify>
    <automated>cargo test -p speck-cli -- validate_and_prepare 2>&1 | head -30</automated>
    <expected>fails with "not found" or "unresolved import" — the function does not yet exist (RED)</expected>
  </verify>
  <done>Tests compile (with errors for missing function) and expected failures are confirmed</done>
</task>

<task type="tdd">
  <name>Task 2: Test GuestConfig ca_certs_paths propagation</name>
  <files>crates/speck-vz/src/config.rs</files>
  <read_first>
    @/Users/piotrek/git/speck/crates/speck-vz/src/config.rs (existing test module at line 479)
  </read_first>
  <behavior>
    - Test 1: Builder with no ca_certs_paths → GuestConfig.ca_certs_paths is empty Vec
    - Test 2: Builder with `.ca_certs_paths(&["/path/to/ca.pem"])` → GuestConfig.ca_certs_paths has the path
    - Test 3: Builder with multiple paths → all paths propagate in order
  </behavior>
  <action>
    Add tests to the existing `#[cfg(test)] mod tests` block at the bottom of `crates/speck-vz/src/config.rs`.

    Follow the existing test patterns: create a temp dir with a dummy kernel, use `GuestConfig::builder().kernel_path(...).ca_certs_paths(...).build()`.

    These tests MUST fail on first run because `ca_certs_paths` doesn't exist on the builder yet.
  </action>
  <verify>
    <automated>cargo test -p speck-vz -- ca_certs 2>&1 | head -20</automated>
    <expected>fails — method not found (RED)</expected>
  </verify>
  <done>Tests compile with errors and expected failures are confirmed</done>
</task>

<task type="tdd">
  <name>Task 3: Test ca_certs_tag in kernel cmdline</name>
  <files>crates/speck-vz/src/virtiofs.rs</files>
  <read_first>
    @/Users/piotrek/git/speck/crates/speck-vz/src/virtiofs.rs (existing test module at line 390)
  </read_first>
  <behavior>
    - Test 1: cmdline_virtiofs_arg with no ca_certs_tag → no "ca_certs_tag=" in output
    - Test 2: cmdline_virtiofs_arg with ca_certs_tag="ca-certs" → output contains "ca_certs_tag=ca-certs"
  </behavior>
  <action>
    Add tests to the existing `#[cfg(test)] mod tests` block at the bottom of `crates/speck-vz/src/virtiofs.rs`.

    Follow existing patterns in the test module. The test should accept that `cmdline_virtiofs_arg` takes an additional parameter `ca_certs_tag: Option<&str>`.

    Tests MUST fail because `cmdline_virtiofs_arg` signature hasn't changed yet.
  </action>
  <verify>
    <automated>cargo test -p speck-vz -- ca_certs_tag 2>&1 | head -20</automated>
    <expected>fails — function signature mismatch or type error (RED)</expected>
  </verify>
  <done>Tests fail as expected (RED), confirming contracts are defined</done>
</task>

</tasks>

<verification>
All three test groups MUST fail (RED) on first execution. If any test compiles and passes, the contract definition was too weak — revisit.
</verification>

<success_criteria>
- `cargo test -p speck-cli -- validate_and_prepare` fails on missing function ✓
- `cargo test -p speck-vz -- ca_certs` fails on missing builder method ✓
- `cargo test -p speck-vz -- ca_certs_tag` fails on missing parameter ✓
- Three RED tests establish the executable contract for Plan 02 and Plan 03
</success_criteria>

<output>
`.planning/phases/12-corporate-ca/12-01-SUMMARY.md` when done
</output>
