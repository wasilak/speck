---
phase: 19-close-testcontainers-conformance-gaps-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/speck-dockerd/src/containerd_client.rs
  - ~/.speck/initrd/initrd.cpio.gz
  - target/aarch64-apple-darwin/release/speck (codesigned)
autonomous: false
requirements: [CONF-05, CONF-06, CONF-03]
must_haves:
  truths:
    - "Container creation via Docker API no longer returns 400 Bad Request — Runtime.Name set correctly"
    - "cargo test -p speck-dockerd --lib passes (unit regression)"
    - "cargo test -p speck-guest passes (guest tests, including RetainedLogBuffer)"
    - "Initrd rebuilt from current guest source includes vminitd with vsock log relay listeners"
    - "Host binary is properly ad-hoc codesigned (com.apple.security.virtualization entitlement present)"
    - "SPECK_TEST_INTEGRATION=1 cargo test -p speck-dockerd --test api_conformance -- --test-threads=2 — all non-ignore tests pass"
  artifacts:
    - path: "crates/speck-dockerd/src/containerd_client.rs"
      provides: "Container Runtime.Name set to io.containerd.runc.v2"
      contains: "runtime: Some(container::Runtime { name: \"io.containerd.runc.v2\""
    - path: "~/.speck/initrd/initrd.cpio.gz"
      provides: "Guest initrd with current vminitd (vsock listeners, bounded log buffer)"
      min_lines: 1
    - path: "target/debug/speck"
      provides: "Codesigned host binary with virtualization entitlement"
  key_links:
    - from: "crates/speck-dockerd/src/containerd_client.rs"
      to: "containerd gRPC Container API"
      via: "tonic client"
      pattern: "runtime: Some"
    - from: "~/.speck/initrd/initrd.cpio.gz"
      to: "crates/speck-guest/src/bin/vminitd.rs"
      via: "guest-cross-compile + initrd packaging"
      pattern: "vminitd"
    - from: "target/debug/speck"
      to: "Virtualization.framework"
      via: "VZVirtualMachine"
      pattern: "com.apple.security.virtualization"
---

<objective>
Fix the single remaining blocker that prevents all testcontainers conformance tests from passing: **containerd's `Container.Runtime.Name` field must be set**. When `runtime: None`, containerd returns `400 Bad Request: container.Runtime.Name must be set`. After fixing, rebuild the initrd from current guest source, codesign the host binary, boot the daemon, and run the full conformance suite end-to-end to prove CONF-05, CONF-06, and CONF-03 work in a live system.

**Purpose:** Unblock the final barrier to testcontainers compatibility. All source code for CONF-05 (follow-stream logs), CONF-06 (HostIp "0.0.0.0" default), and CONF-03 (network 404 validation) was committed by Phases 17 and 17.1 but has NEVER been exercised end-to-end — every conformance integration test fails at container create with 400 Bad Request.

**Output:** 
- `containerd_client.rs` — Runtime.Name fix committed
- Rebuilt initrd at `~/.speck/initrd/initrd.cpio.gz`
- Codesigned host binary at `target/debug/speck`
- Green run of all 7 non-`#[ignore]` conformance tests
- Any additional fixes discovered by the live suite
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-close-testcontainers-conformance-gaps-inserted/19-RESEARCH.md
@crates/speck-dockerd/src/containerd_client.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix containerd Container.Runtime.Name in containerd_client.rs</name>
  <files>
    crates/speck-dockerd/src/containerd_client.rs
  </files>
  <read_first>
    crates/speck-dockerd/src/containerd_client.rs (lines 1-30 for imports, 220-250 for the Container literal)
  </read_first>
  <action>
    In `crates/speck-dockerd/src/containerd_client.rs`:

    1. Add this import to the existing `use containerd_client::services::v1::{...}` block at the top of the file:
       Add: `container::Runtime as ContainerRuntime,`
       The import line already includes `Container` — this adds the nested `container::Runtime` type alias.
       Final import line should read:
       ```
       use containerd_client::services::v1::{
           container::Runtime as ContainerRuntime,
           Container, CreateContainerRequest, CreateTaskRequest, ...
       };
       ```

    2. Change line ~226:
       From: `runtime: None,`
       To: `runtime: Some(ContainerRuntime { name: "io.containerd.runc.v2".to_string(), options: None }),`

    3. Verify `prost-types` is already in `crates/speck-dockerd/Cargo.toml` (confirmed: `prost-types = "0.14.4"` at line 16) — the `options: None` field type-checks without an explicit `prost_types::Any` import because `None` works for any `Option<T>`.

    4. Run regression: `cargo test -p speck-dockerd --lib` and `cargo test -p speck-guest` — both must pass.
  </action>
  <verify>
    <automated>cargo test -p speck-dockerd --lib && cargo test -p speck-guest</automated>
  </verify>
  <acceptance_criteria>
    - containerd_client.rs line ~226 reads `runtime: Some(ContainerRuntime { name: "io.containerd.runc.v2".to_string(), options: None })`
    - Import line includes `container::Runtime as ContainerRuntime`
    - `cargo test -p speck-dockerd --lib` exits 0 (all lib tests pass)
    - `cargo test -p speck-guest` exits 0 (all guest tests pass)
    - `cargo build` exits 0 (full workspace build clean)
  </acceptance_criteria>
  <done>
    Runtime.Name fix committed; container creation no longer returns 400 Bad Request from containerd validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild initrd from current guest source + codesign host binary</name>
  <files>
    ~/.speck/initrd/initrd.cpio.gz
    target/debug/speck
  </files>
  <read_first>
    scripts/provision-guest-resize-tools.sh (lines 1-50 for the unpack/pack workflow)
    crates/speck-dockerd/src/containerd_client.rs (already read in Task 1)
  </read_first>
  <action>
    Execute the full build pipeline in order:

    Step 1 — Verify toolchain:
    ```
    rustup target list --installed | grep aarch64-unknown-linux-musl
    command -v aarch64-linux-musl-gcc
    ```
    If target not installed: `rustup target add aarch64-unknown-linux-musl`
    If linker not found: `brew install aarch64-unknown-linux-musl` (or equivalent)

    Step 2 — Build guest (musl static binary):
    ```
    GUEST_TARGET="aarch64-unknown-linux-musl"
    export CARGO_TARGET_AARCH64_UNKNOWN_LINUX_MUSL_LINKER=aarch64-linux-musl-gcc
    cargo build --release -p speck-guest --target "$GUEST_TARGET"
    ```

    Step 3 — Package initrd using the project's provisioning script:
    The guest binary from step 2 needs to be injected into the initrd alongside the resize tools.
    Run: `bash scripts/provision-guest-resize-tools.sh`
    Verify: `bash scripts/check-guest-resize-tools.sh`
    This script unpacks the base initrd, adds the newly-built guest binaries (vminitd, log_relay components), adds resize2fs/mke2fs/blkid from Alpine APK (via skopeo fallback), and repacks to `~/.speck/initrd/initrd.cpio.gz`.

    Step 4 — Build host debug binary:
    ```
    cargo build -p speck-cli
    ```

    Step 5 — Codesign:
    ```
    cargo xtask codesign-dev
    ```
    Verify codesign: `codesign --verify --verbose target/debug/speck` exits 0.
    Verify entitlement: `codesign -d --entitlements - target/debug/speck 2>/dev/null | grep -c com.apple.security.virtualization` outputs 1.

    Step 6 — Quick smoke test:
    ```
    cargo run -- up --foreground &
    sleep 15
    curl --unix-socket ~/.speck/speck.sock http://localhost/_ping
    ```
    Expect `OK` response. Then stop the daemon: `kill %1` (or `spk down`).

    If `spk up` fails (e.g., kernel/assets not downloaded), run `cargo run -- up --pull` first, then retry.

    Note: The daemon port-probe wait is automatically handled. If the VM boot takes longer than 15s, increase sleep to 30s or check `~/.speck/console.log` for boot progress.
  </action>
  <verify>
    <automated>codesign -d --entitlements - target/debug/speck 2>/dev/null | grep -q com.apple.security.virtualization</automated>
  </verify>
  <acceptance_criteria>
    - `codesign --verify --verbose target/debug/speck` exits 0
    - `codesign -d --entitlements - target/debug/speck 2>&1 | grep com.apple.security.virtualization` shows `<true/>`
    - `curl --unix-socket ~/.speck/speck.sock http://localhost/_ping` returns `OK` after `spk up`
    - `bash scripts/check-guest-resize-tools.sh` shows "guest resize tools are present"
  </acceptance_criteria>
  <done>
    Rebuilt initrd deployed, host binary codesigned, daemon boots and responds to `_ping`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run full conformance suite + fix any remaining failures</name>
  <files>
    crates/speck-dockerd/tests/api_conformance.rs
    (plus any files needing fixes identified by test failures)
  </files>
  <read_first>
    crates/speck-dockerd/tests/api_conformance.rs (lines 1-50 for the SPECK_TEST_INTEGRATION guard pattern)
    crates/speck-dockerd/src/containerd_client.rs (for any Runtime-related follow-up fixes)
    crates/speck-dockerd/src/handlers/containers.rs (for HostIp/inspect follow-up)
    crates/speck-dockerd/src/handlers/attach.rs (for follow-stream follow-up)
  </read_first>
  <action>
    Boot daemon with `spk up --foreground`. Run SPECK_TEST_INTEGRATION=1 conformance tests. Fix any failures found by diagnosing root cause, rebuilding codesigned binary, restarting daemon, and re-running. Repeat until all 7 non-ignore tests pass. Then run the #[ignore] suite and document results.
  </action>
  <verify>
    <automated>SPECK_TEST_INTEGRATION=1 cargo test -p speck-dockerd --test api_conformance -- --test-threads=2</automated>
  </verify>
  <what-built>
    Fix for Runtime.Name committed. Initrd rebuilt from current guest source. Host binary codesigned.
  </what-built>
  <how-to-verify>
    The daemon from Task 2 must be running (`spk up --foreground`) in a separate terminal.

    1. Set up the integration environment:
       ```
       export SPECK_TEST_INTEGRATION=1
       export SPECK_SOCK="$HOME/.speck/speck.sock"
       ```

    2. Run the 7 non-`#[ignore]` conformance tests:
       ```
       cargo test -p speck-dockerd --test api_conformance -- --test-threads=2
       ```
       Expected: All 7 non-ignore tests pass:
       - `test_logs_follow_streams_delayed_output` — verifies CONF-05: follow-stream receives bytes written after subscription
       - `test_inspect_default_host_ip` — verifies CONF-06: omitted HostIp → "0.0.0.0"
       - `test_inspect_shows_port_bindings` — verifies CONF-06: port bindings present in inspect
       - `test_inspect_empty_string_host_ip_defaults` — verifies CONF-06: empty-string HostIp → "0.0.0.0"
       - `test_network_connect_missing_network_404` — verifies CONF-03
       - `test_network_disconnect_missing_network_404` — verifies CONF-03
       - `test_network_connect_missing_container_404` — verifies CONF-03
       - `test_container_wait_exit_code` — verifies CONF-07

    3. If any test FAILS:
       - Read the failure output carefully
       - Diagnose the root cause (is it the Runtime.Name fix? Is it a pre-existing issue?)
       - Fix the underlying source code
       - Rebuild: `cargo build -p speck-cli && cargo xtask codesign-dev`
       - Restart daemon (`spk down && spk up --foreground`)
       - Re-run the failing test(s)
       - Repeat until all 7 tests pass
       - Commit each fix with a descriptive message

    4. If all 7 tests pass, also run the full `#[ignore]` suite (requires pre-pulled images like alpine, nginx:alpine):
       ```
       cargo test -p speck-dockerd --test api_conformance -- --ignored --include-ignored --test-threads=1
       ```
       Note: `test_port_publish_nginx` and `test_dns_resolution_inside_container` require nginx:alpine image and external network. These may be pre-pulled or pulled on demand. If a test hangs due to image pull, skip it and continue. Report which `#[ignore]` tests pass/fail.

    5. After all tests pass, stop the daemon:
       ```
       spk down
       ```
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 non-ignore conformance tests pass. Or describe which tests failed and what fixes were needed.</resume-signal>
  <acceptance_criteria>
    - `SPECK_TEST_INTEGRATION=1 cargo test -p speck-dockerd --test api_conformance -- --test-threads=2` exits 0 with all 7 non-ignore tests passing
    - Any discovered bugs are fixed and committed
    - `#[ignore]` suite results documented (pass/fail per test)
  </acceptance_criteria>
  <done>
    Full conformance suite green against live daemon with rebuilt initrd. CONF-05, CONF-06, CONF-03 end-to-end verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SpeckDockerd → containerd gRPC | Configuration values cross from host Rust code into guest containerd's gRPC API |
| Codesign verification | Binary integrity check before Virtualization.framework invocation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | containerd_client.rs `Runtime.Name` | accept | `"io.containerd.runc.v2"` is a hardcoded configuration constant — no user input, no injection vector. containerd validates the value against registered runtimes; an invalid name produces a deterministic error, not a security breach. |
| T-19-02 | Tampering | initrd packaging | accept | The initrd is built from local source and signed binaries from the build system. No external download or unverified content is injected during the build pipeline. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are introduced by this phase. `cargo xtask codesign-dev` uses only the built `target/debug/speck` binary. Slopcheck N/A. |
</threat_model>

<verification>
1. `cargo test -p speck-dockerd --lib` — unit regression for Runtime.Name fix (pass)
2. `cargo test -p speck-guest` — guest tests including RetainedLogBuffer (pass)
3. `codesign -d --entitlements - target/debug/speck` — entitlement verification (com.apple.security.virtualization present)
4. `curl --unix-socket ~/.speck/speck.sock http://localhost/_ping` — daemon responds OK
5. `SPECK_TEST_INTEGRATION=1 cargo test -p speck-dockerd --test api_conformance -- --test-threads=2` — all 7 non-ignore tests pass
</verification>

<success_criteria>
1. Container creation no longer returns 400 Bad Request — Runtime.Name fix committed and unit-tested
2. Initrd rebuilt from current guest source, including vminitd with vsock log relay listeners
3. Host binary ad-hoc codesigned with `com.apple.security.virtualization` entitlement
4. All 7 non-`#[ignore]` conformance tests pass against live daemon
5. Any additional bugs found by the live suite are fixed and committed
</success_criteria>

<artifacts_this_phase_produces>
**Symbols created:**
- Import: `containerd_client::services::v1::container::Runtime as ContainerRuntime` (in `containerd_client.rs`)
- Struct literal: `ContainerRuntime { name: "io.containerd.runc.v2", options: None }` (in `containerd_client.rs` line ~226)

**Files modified:**
- `crates/speck-dockerd/src/containerd_client.rs` — Runtime.Name field changed from `None` to `Some(...)`

**Build artifacts:**
- `~/.speck/initrd/initrd.cpio.gz` — Rebuilt initrd from current guest source
- `target/debug/speck` — Codesigned host binary with virtualization entitlement

**Tests verified:**
- 7 non-`#[ignore]` conformance integration tests in `crates/speck-dockerd/tests/api_conformance.rs`
- Up to 15 `#[ignore]` conformance integration tests
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/19-close-testcontainers-conformance-gaps-inserted/19-01-SUMMARY.md` when done
</output>
