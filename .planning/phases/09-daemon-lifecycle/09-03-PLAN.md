---
phase: 09-daemon-lifecycle
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/speck-cli/src/commands/down.rs
autonomous: true
requirements:
  - DAEMON-03
  - DAEMON-04
must_haves:
  truths:
    - "spk down connects to $SPECK_HOME/run/control.sock and reads PONG before issuing bootout (D2)"
    - "spk down calls launchctl bootout gui/{uid}/io.speck.vm to trigger SIGTERM on the daemon (D4)"
    - "spk down removes ~/Library/LaunchAgents/io.speck.vm.plist after bootout so the daemon does not auto-register on next login (D4)"
    - "spk down polls until $SPECK_HOME/run/control.sock disappears (timeout 30s) confirming graceful shutdown (D2)"
    - "spk down exits with a clear error if the control socket is not reachable (daemon is not running)"
    - "DAEMON-04 (restart) is satisfied by the user running spk down && spk up — no spk restart subcommand is added"
  artifacts:
    - path: "crates/speck-cli/src/commands/down.rs"
      provides: "full run_down implementation"
      contains: "run/control.sock"
  key_links:
    - from: "crates/speck-cli/src/commands/down.rs"
      to: "$SPECK_HOME/run/control.sock"
      via: "tokio::net::UnixStream::connect"
      pattern: "control.sock"
    - from: "crates/speck-cli/src/commands/down.rs"
      to: "~/Library/LaunchAgents/io.speck.vm.plist"
      via: "std::fs::remove_file"
      pattern: "io.speck.vm.plist"
---

<objective>
Implement `spk down` fully: connect to the control socket to confirm daemon liveness, signal it to stop via `launchctl bootout`, remove the plist to prevent auto-restart, and wait for clean shutdown.

Purpose: The current `down.rs` is a 7-line stub that only prints a message. This plan replaces it with the real four-step shutdown sequence per D4: liveness check → bootout → plist removal → socket disappearance poll.

Output: `commands/down.rs` with complete `run_down` implementation and source-inspection tests.

DAEMON-04 NOTE: CONTEXT.md §Deferred explicitly excludes `spk restart` as a first-class command from Phase 09. DAEMON-04 ("performs a graceful stop followed by a fresh start") is met by the documented user workflow `spk down && spk up`. No `restart` subcommand is created. Verifiers should accept `spk down && spk up` as the DAEMON-04 evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-daemon-lifecycle/09-CONTEXT.md
@.planning/phases/09-daemon-lifecycle/09-RESEARCH.md
@.planning/phases/09-daemon-lifecycle/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement full run_down in down.rs with source-inspection tests</name>
  <files>
    crates/speck-cli/src/commands/down.rs
  </files>
  <read_first>
    - crates/speck-cli/src/commands/down.rs — the current 7-line stub (full replacement); read the function signature: `pub async fn run_down(_speck_home: &Path) -> anyhow::Result<()>`
    - crates/speck-cli/src/commands/stop.rs — command function signature shape; imports of `use anyhow::Context as _` and function signature convention
    - .planning/phases/09-daemon-lifecycle/09-RESEARCH.md §Pattern 4 — the complete run_down design: connect to socket, read PONG, bootout, remove plist, poll for socket file disappearance
    - .planning/phases/09-daemon-lifecycle/09-PATTERNS.md §down.rs section — tokio::process::Command async child process pattern from up.rs lines 118-125; polling loop design
    - crates/speck-cli/src/commands/up.rs lines 118-125 — tokio::process::Command async pattern: `.status().await.context("...")?;`
  </read_first>
  <behavior>
    - run_down: Step 1 liveness — tokio::net::UnixStream::connect(sock_path).await with .context("daemon is not running — start with spk up"); reads up to 8 bytes (must receive response, exact content not checked); drops the stream
    - run_down: Step 2 bootout — get UID via `std::process::Command::new("id").arg("-u").output()`, trim stdout to uid_str; run `tokio::process::Command::new("launchctl").args(["bootout", &format!("gui/{uid_str}/io.speck.vm")]).status().await`; non-fatal if bootout fails (daemon may already be stopping)
    - run_down: Step 3 plist removal — build plist_path from `std::env::var("HOME").context("HOME not set")?.join("Library/LaunchAgents/io.speck.vm.plist")`; call `std::fs::remove_file(&plist_path)` — ignore NotFound errors, propagate others; removing prevents daemon auto-registration on next login
    - run_down: Step 4 poll — loop with deadline = Instant::now() + Duration::from_secs(30); check sock_path.exists() every 200ms via tokio::time::sleep; exit loop when file disappears or deadline passes; if deadline passes, log tracing::warn!("daemon did not stop within 30s")
    - run_down: Step 5 output — `println!("Speck stopped.")` on success path
    - Constant LAUNCHD_LABEL: &str = "io.speck.vm" declared at module top (used in format string `gui/{uid_str}/{LAUNCHD_LABEL}`)
    - Source-inspection tests using include_str!("down.rs"):
      - down_connects_to_control_socket: source contains both "control.sock" and "UnixStream::connect"
      - down_uses_launchctl_bootout: source contains "bootout" and LAUNCHD_LABEL ("io.speck.vm")
      - down_removes_plist_on_shutdown: source contains "io.speck.vm.plist" and "remove_file"
      - down_polls_for_socket_disappearance: source contains "30" (30-second timeout) and "200" (200ms sleep)
      - down_daemon04_met_by_down_then_up: test body contains a comment asserting DAEMON-04 is met by spk down && spk up, and asserts source does NOT contain "restart" as a subcommand name
  </behavior>
  <action>
    Replace the full content of crates/speck-cli/src/commands/down.rs.

    IMPORTS: use std::path::{Path, PathBuf}; use std::time::{Duration, Instant}; use anyhow::Context as _; use tokio::io::AsyncReadExt; use tokio::net::UnixStream;

    CONSTANT at top: const LAUNCHD_LABEL: &str = "io.speck.vm";

    FUNCTION pub async fn run_down(speck_home: &Path) -> anyhow::Result<()> with four numbered steps:

    Step 1 — Liveness check: let sock_path = speck_home.join("run/control.sock"); let mut stream = UnixStream::connect(&sock_path).await .context("daemon is not running (control socket not reachable — start with spk up)")?; let mut buf = [0u8; 8]; let _ = stream.read(&mut buf).await; drop(stream); tracing::info!("daemon is alive, proceeding with shutdown");

    Step 2 — Issue bootout (triggers SIGTERM → daemon's graceful shutdown): get uid_str by running std::process::Command::new("id").arg("-u").output().context("failed to run id -u")?, parsing stdout with std::str::from_utf8(&out.stdout).context("non-UTF8 uid")?.trim().to_owned(); let _status = tokio::process::Command::new("launchctl") .args(["bootout", &format!("gui/{uid_str}/{LAUNCHD_LABEL}")]) .status().await .context("launchctl bootout failed")?; (non-fatal: if bootout returns non-zero, log tracing::warn! but do not propagate error)

    Step 3 — Remove plist (prevent auto-registration on next login): let home = std::env::var("HOME").context("HOME not set")?; let plist_path = PathBuf::from(home).join("Library/LaunchAgents").join(format!("{LAUNCHD_LABEL}.plist")); match std::fs::remove_file(&plist_path) { Ok(()) => tracing::info!("plist removed"), Err(e) if e.kind() == std::io::ErrorKind::NotFound => {}, Err(e) => tracing::warn!(error = %e, "failed to remove plist"), }

    Step 4 — Poll for clean shutdown: let deadline = Instant::now() + Duration::from_secs(30); while sock_path.exists() { if Instant::now() >= deadline { tracing::warn!("daemon did not stop within 30 seconds"); break; } tokio::time::sleep(Duration::from_millis(200)).await; }

    Step 5 — User output: println!("Speck stopped.");

    TESTS (#[cfg(test)] mod tests at bottom of file): const DOWN_SOURCE: &str = include_str!("down.rs");

    test down_connects_to_control_socket: assert DOWN_SOURCE contains "control.sock" and "UnixStream::connect"

    test down_uses_launchctl_bootout: assert DOWN_SOURCE contains "bootout" and "io.speck.vm"

    test down_removes_plist_on_shutdown: assert DOWN_SOURCE contains "io.speck.vm.plist" and "remove_file"

    test down_polls_for_socket_disappearance: assert DOWN_SOURCE contains "from_secs(30)" and "from_millis(200)"

    test down_daemon04_met_by_down_then_up: assert DOWN_SOURCE does NOT contain a string "Commands::Restart" or fn run_restart — DAEMON-04 is met by the user workflow `spk down && spk up`, not a restart subcommand; the test documents this by asserting the absence of a restart command in this file

    Ensure all tracing calls use tracing::info! or tracing::warn! (not println! for lifecycle events). The final "Speck stopped." user message uses println! per CLI convention.
  </action>
  <verify>
    <automated>cargo test -p speck-cli --lib -- commands::down::tests 2>&1 | tail -20 && cargo build -p speck-cli 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - crates/speck-cli/src/commands/down.rs contains `const LAUNCHD_LABEL: &str = "io.speck.vm";`
    - crates/speck-cli/src/commands/down.rs contains `run/control.sock` path construction
    - crates/speck-cli/src/commands/down.rs contains `bootout`
    - crates/speck-cli/src/commands/down.rs contains `io.speck.vm.plist`
    - crates/speck-cli/src/commands/down.rs contains `from_secs(30)` (shutdown timeout)
    - crates/speck-cli/src/commands/down.rs contains `from_millis(200)` (poll interval)
    - `cargo test -p speck-cli --lib -- commands::down::tests` exits 0 with 5 tests passing
    - `cargo build -p speck-cli` exits 0
    - Source does NOT contain a `run_restart` function or `Commands::Restart` enum variant in this file
  </acceptance_criteria>
  <done>run_down fully implemented, all 5 source-inspection tests pass, cargo build clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spk down → control socket | UnixStream connect to daemon-owned socket; any local process can attempt this |
| run_down → launchctl | Child process invocation on fixed label; no user-controlled input in the label |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | control.sock connect | accept | Socket created by daemon with 0o600 permissions (enforced in plan 09-02); only the owning user can connect; no command channel exposed — ping-pong only |
| T-09-04 | Elevation of Privilege | launchd label in bootout | accept | Label is a compile-time constant `io.speck.vm`; no user input is interpolated into the launchctl args |
| T-09-05 | Tampering | plist removal | accept | Plist path is computed from HOME + fixed filename; no user-controlled components |
</threat_model>

<verification>
After task completes:
- `cargo test -p speck-cli` exits 0 (all existing tests plus 5 new down tests pass)
- `cargo build -p speck-cli` exits 0
- `rg "const LAUNCHD_LABEL" crates/speck-cli/src/commands/down.rs` exits 0
- `rg "bootout" crates/speck-cli/src/commands/down.rs` exits 0
- `rg "io.speck.vm.plist" crates/speck-cli/src/commands/down.rs` exits 0
</verification>

<success_criteria>
- run_down performs the four-step shutdown: liveness check → bootout → plist removal → socket poll
- All 5 source-inspection tests pass confirming structural requirements are met
- DAEMON-04 documented as satisfied by `spk down && spk up` user workflow
</success_criteria>

<output>
Create `.planning/phases/09-daemon-lifecycle/09-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

New symbols created by this plan (exclude from drift verification):

| Symbol | Kind | File |
|--------|------|------|
| `LAUNCHD_LABEL` | const (&str) | `crates/speck-cli/src/commands/down.rs` |
| `run_down` | pub async fn (full impl, replaces stub) | `crates/speck-cli/src/commands/down.rs` |
