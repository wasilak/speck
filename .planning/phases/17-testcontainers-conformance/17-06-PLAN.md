---
phase: 17-testcontainers-conformance
plan: 06
type: execute
wave: 3
depends_on:
  - 17-03
  - 17-04
  - 17-05
files_modified:
  - crates/speck-dockerd/tests/api_conformance.rs
autonomous: true
requirements:
  - CONF-07

must_haves:
  truths:
    - "Three bollard conformance tests (test_container_logs, test_inspect_shows_port_bindings, test_container_wait_exit_code) compile and pass with #[ignore] removed"
    - "Each test exercises the full Docker API path through SpeckDockerd"
  artifacts:
    - path: "crates/speck-dockerd/tests/api_conformance.rs"
      provides: "Three enabled conformance tests, all #[ignore] attributes removed, proper timeouts for integration tests"
      contains: "test_container_logs"
  key_links:
    - from: "test_container_logs"
      to: "container_logs_handler"
      via: "bollard client sends GET /containers/{id}/logs"
      pattern: "logs"
    - from: "test_inspect_shows_port_bindings"
      to: "container_inspect_json"
      via: "bollard client sends GET /containers/{id}/json"
      pattern: "inspect"
    - from: "test_container_wait_exit_code"
      to: "container_wait_handler"
      via: "bollard client sends POST /containers/{id}/wait"
      pattern: "wait"
---

<objective>
Enable the three testcontainers conformance tests (CONF-07) by removing `#[ignore]`, adding appropriate timeouts, and verifying the full Docker API path from bollard client through SpeckDockerd.

**Purpose:** When `make test` (or `cargo test`) runs, these three tests execute and pass, proving that SpeckDockerd correctly handles the Docker API operations that testcontainers depends on: logs, inspect with port bindings, and container wait with exit code.

**Output:** Updated `tests/api_conformance.rs`.
</objective>

<execution_context>
@/Users/piotrek/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17-testcontainers-conformance/17-RESEARCH.md
@.planning/phases/17-testcontainers-conformance/17-PATTERNS.md
@crates/speck-dockerd/tests/api_conformance.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable and fix three conformance tests</name>
  <files>
    - crates/speck-dockerd/tests/api_conformance.rs
  </files>
  <read_first>
    - crates/speck-dockerd/tests/api_conformance.rs (all 14+ tests, imports, test helpers, setup)
  </read_first>

  <action>
    In `tests/api_conformance.rs`:

    **A. Analyze current state of target tests**: Search for `test_container_logs`, `test_inspect_shows_port_bindings`, `test_container_wait_exit_code`. Read each carefully:

    - What do they do? (create container, start, wait, inspect, etc.)
    - What helpers/setup do they use? (`setup_docker_client()`, `setup_test_container()`, etc.)
    - What do they assert?
    - What `#[ignore]` / `#[should_panic]` / `#[cfg(...)]` attributes are on them?
    - Are they integration tests (need a running VM/daemon)?

    **B. Remove `#[ignore]`** from all three tests. If they have other test attributes like `#[cfg(integration)]` or `#[cfg(feature = "integration")]`, keep those — only remove `#[ignore]`.

    **C. Add timeouts for integration safety**: Add a `tokio::time::timeout` wrapper or an explicit timeout via `#[tokio::test]` attribute to prevent hanging:

    For async tests:
    ```rust
    #[tokio::test(flavor = "multi_thread")]
    async fn test_container_logs() {
        // existing code
    }
    ```
    → Add explicit timeout:
    ```rust
    use tokio::time::{timeout, Duration};

    #[tokio::test(flavor = "multi_thread")]
    async fn test_container_logs() {
        let result = timeout(Duration::from_secs(30), async {
            // existing test body
        }).await;
        assert!(result.is_ok(), "test timed out");
    }
    ```
    If the test already has helper functions, transform them similarly.

    **D. Fix test logic issues**: Read each test's assertions. Common issues:
    - Tests that expect port bindings may fail if the test doesn't set `HostConfig.PortBindings` on the create call
    - Log tests may fail if the test container produces no output
    - Wait tests may fail if container exit code can't be read

    For each test, verify:
    1. The container it creates actually produces the expected data (e.g., `echo "hello"` for logs, exposes a port for port bindings)
    2. The assertions match what SpeckDockerd actually returns

    **E. If a test needs infrastructure that isn't set up** (e.g., requires a running VM with containerd), add a guard at the top of the test:
    ```rust
    if std::env::var("SPECK_TEST_INTEGRATION").is_err() {
        eprintln!("skipping integration test: SPECK_TEST_INTEGRATION not set");
        return;
    }
    ```

    **F. Make sure all 14+ tests still compile** — only remove `#[ignore]` from the three target tests. Do not modify other tests.

    **G. Run the test compile check** — this does not execute the tests (integration tests need a running VM), but they must compile:
    ```bash
    cargo test -p speck-dockerd --test api_conformance --no-run
    ```
  </action>

  <verify>
    <automated>cargo test -p speck-dockerd --test api_conformance --no-run 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `test_container_logs`: `#[ignore]` removed, compiles, has 30s timeout
    - `test_inspect_shows_port_bindings`: `#[ignore]` removed, compiles, has 30s timeout
    - `test_container_wait_exit_code`: `#[ignore]` removed, compiles, has 30s timeout
    - All three tests have appropriate integration guards if they need a running VM
    - All other tests in the file unchanged
    - `cargo test --no-run` compiles the test file successfully
  </acceptance_criteria>

  <done>
    Three conformance tests compiled (no #[ignore]) and ready for integration execution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → SpeckDockerd | bollard client connects to Unix socket — same as production path |
| SpeckDockerd → containerd (in-guest) | gRPC over vsock — existing trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Tampering | Integration test timeout misconfigured | mitigate | 30s timeouts prevent indefinite hangs. Cargo test runner's own timeout catches runaway tests. |
</threat_model>

<verification>
- `cargo test -p speck-dockerd --test api_conformance --no-run` compiles
- All tests that pass in `cargo test` (unit tests, non-integration) still pass
</verification>

<success_criteria>
- Three conformance tests have `#[ignore]` removed, proper timeouts, and compile successfully
- `cargo test -p speck-dockerd` still passes all unit tests
</success_criteria>

<output>
Create `.planning/phases/17-testcontainers-conformance/17-06-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

**Modified files:**
- `crates/speck-dockerd/tests/api_conformance.rs` — three tests enabled, `#[ignore]` removed, timeouts added
</plan>
