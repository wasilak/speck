---
phase: 11-vpn-proof-dns
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/speck-net/tests/dns_split_test.rs
  - crates/speck-net/src/dns.rs
autonomous: true
requirements:
  - DNS-04
  - DNS-05
  - DNS-06

must_haves:
  truths:
    - "crates/speck-net/tests/dns_split_test.rs exists with 5 tests covering ResolverTable behaviors (DNS-04, DNS-05)"
    - "crates/speck-net/src/dns.rs has a #[cfg(test)] mod tests block with 4 RCODE rewrite tests (DNS-06)"
    - "cargo build -p speck-net exits 0 — #[cfg(test)] blocks do not affect the production build"
    - "cargo test -p speck-net fails to compile with errors naming resolver_table and translate_nxdomain_to_servfail — expected RED baseline"
  artifacts:
    - path: crates/speck-net/tests/dns_split_test.rs
      provides: Failing test stubs for ResolverTable (empty table, exact match, suffix match, longest suffix wins, no match)
      contains: "empty_table_returns_none"
    - path: crates/speck-net/src/dns.rs
      provides: Inline failing test stubs for NXDOMAIN to SERVFAIL rewrite
      contains: "#[cfg(test)]"
  key_links: []
---

<objective>
Establish the TDD RED baseline for Phase 11 behaviors before any implementation exists.

Purpose: Define behavioral contracts for ResolverTable (DNS-04/DNS-05) and NXDOMAIN→SERVFAIL
         rewrite (DNS-06) as failing tests. Plans 11-03 and 11-04 will turn these GREEN.
         Having the contracts committed first prevents implementation drift.

Output: crates/speck-net/tests/dns_split_test.rs (new), crates/speck-net/src/dns.rs (test block appended)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-vpn-proof-dns/11-RESEARCH.md
@.planning/phases/11-vpn-proof-dns/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResolverTable test stubs in dns_split_test.rs</name>
  <files>crates/speck-net/tests/dns_split_test.rs</files>
  <read_first>
    - crates/speck-net/src/lib.rs — verify current public exports before writing imports; resolver_table is NOT yet exported
    - crates/speck-net/Cargo.toml — confirm [dev-dependencies] setup; no extra deps needed for this test file
    - .planning/phases/11-vpn-proof-dns/11-PATTERNS.md — ResolverTable public API: add_entry(String, Vec&lt;IpAddr&gt;), find_resolver(&amp;str) -&gt; Option&lt;&amp;[IpAddr]&gt;
  </read_first>
  <behavior>
    - Test empty_table_returns_none: ResolverTable::default().find_resolver("example.com") == None
    - Test exact_match_returns_servers: add_entry("corp", vec![127.0.0.1]); find_resolver("corp") == Some([127.0.0.1])
    - Test suffix_match_single_label: add_entry("corp", vec![10.0.0.1]); find_resolver("internal.corp") == Some([10.0.0.1])
    - Test longest_suffix_wins: add_entry("example.com", [1.1.1.1]); add_entry("corp.example.com", [10.0.0.1]); find_resolver("host.corp.example.com") == Some([10.0.0.1])
    - Test no_match_returns_none: add_entry("corp", [10.0.0.1]); find_resolver("example.com") == None
  </behavior>
  <action>
    Create the directory crates/speck-net/tests/ (it does not exist) and the file dns_split_test.rs.

    The file top-level imports: `use speck_net::resolver_table::ResolverTable;` and `use std::net::IpAddr;`. The module resolver_table does not exist yet in speck-net — this WILL fail to compile until Plan 11-03 creates it. That compile failure is the expected RED state for Wave 0.

    Write the five #[test] functions named exactly: empty_table_returns_none, exact_match_returns_servers, suffix_match_single_label, longest_suffix_wins, no_match_returns_none.

    Each test uses: `ResolverTable::default()`, `.add_entry(domain.to_string(), servers)` where servers is Vec&lt;IpAddr&gt;, and `.find_resolver("domain")` returning Option&lt;&amp;[IpAddr]&gt;. Assert using assert_eq! or assert!(x.is_none()) / assert!(x.is_some()).

    Do not import anything beyond ResolverTable and IpAddr. Do not add build.rs or feature flags.
  </action>
  <verify>
    <automated>cargo build -p speck-net 2>&1; echo "build_exit=$?"</automated>
    Expected: build_exit=0 (production code unaffected — test file is separate crate).
    Then: cargo test -p speck-net 2>&1 | grep -c "resolver_table"
    Expected: count >= 1 (compile error mentioning resolver_table confirms RED baseline).
  </verify>
  <acceptance_criteria>
    - File crates/speck-net/tests/dns_split_test.rs exists
    - File contains exactly these function names: empty_table_returns_none, exact_match_returns_servers, suffix_match_single_label, longest_suffix_wins, no_match_returns_none
    - cargo build -p speck-net exits 0
    - cargo test -p speck-net output contains "resolver_table" in error text (RED baseline)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RCODE rewrite test stubs to dns.rs</name>
  <files>crates/speck-net/src/dns.rs</files>
  <read_first>
    - crates/speck-net/src/dns.rs — read the full file (215 lines); append test block after the closing brace of build_servfail_response (line 215)
    - .planning/phases/11-vpn-proof-dns/11-PATTERNS.md — dns.rs test block structure: use super::*; tests call super::translate_nxdomain_to_servfail
    - .planning/phases/11-vpn-proof-dns/11-RESEARCH.md — Pattern 4: RFC 1035 wire format, byte 3 lower 4 bits = RCODE; NXDOMAIN=3, SERVFAIL=2, NOERROR=0
  </read_first>
  <behavior>
    - Test nxdomain_to_servfail_rewrites_rcode: 12-byte buf with byte 3 = 0x03; after translate, byte 3 &amp; 0x0f == 2
    - Test servfail_passthrough_unchanged: 12-byte buf with byte 3 = 0x02; after translate, byte 3 &amp; 0x0f == 2 (no change)
    - Test noerror_not_touched: byte 3 = 0x00; after translate, byte 3 &amp; 0x0f == 0 (only NXDOMAIN is rewritten)
    - Test too_short_does_not_panic: 3-byte buf; translate must return without panic (early return guard)
  </behavior>
  <action>
    Append to crates/speck-net/src/dns.rs a #[cfg(test)] mod tests block after the final closing brace of build_servfail_response.

    The block starts with `use super::*;`. The four tests (named exactly as listed in behavior) call `translate_nxdomain_to_servfail(&mut buf)`. The function translate_nxdomain_to_servfail does NOT exist yet in dns.rs — this test block will fail to compile during `cargo test` until Plan 11-04 adds the real function. That compile failure IS the expected RED state.

    For tests that need a 12-byte buffer: `let mut buf = vec![0u8; 12]; buf[3] = 0x03;`. For the panic test: `let mut buf = vec![0u8; 3];`. Use `assert_eq!(buf[3] &amp; 0x0f, expected_rcode)` after calling the function.

    Do NOT add any new imports to the non-test portion of dns.rs. Do NOT add any use statements beyond `use super::*;` inside the test module.
  </action>
  <verify>
    <automated>cargo build -p speck-net 2>&1; echo "build_exit=$?"</automated>
    Expected: build_exit=0 (cfg(test) is not compiled by cargo build).
    Then: cargo test -p speck-net 2>&1 | grep -c "translate_nxdomain"
    Expected: count >= 1 (compile error confirms RED baseline for dns.rs tests).
  </verify>
  <acceptance_criteria>
    - crates/speck-net/src/dns.rs ends with a #[cfg(test)] mod tests block containing the four named functions
    - cargo build -p speck-net exits 0
    - cargo test -p speck-net output contains "translate_nxdomain_to_servfail" in error text (RED baseline)
    - No changes to any function outside the #[cfg(test)] block
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code / production | #[cfg(test)] blocks must not bleed into production binary; cargo build --release must be clean |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-SC | Tampering | No cargo deps added in this plan | n/a | Wave 0 adds no new packages; no slopcheck gate needed here |
</threat_model>

<verification>
After both tasks complete:
- cargo build -p speck-net exits 0
- cargo test -p speck-net exits non-zero with TWO compile errors: "resolver_table" (from tests/) and "translate_nxdomain_to_servfail" (from dns.rs #[cfg(test)])
- crates/speck-net/tests/dns_split_test.rs exists
- dns.rs contains "#[cfg(test)]" at end of file
</verification>

<success_criteria>
1. crates/speck-net/tests/dns_split_test.rs exists with 5 named test functions for ResolverTable
2. crates/speck-net/src/dns.rs has a #[cfg(test)] mod tests block at end with 4 RCODE rewrite tests
3. cargo build -p speck-net exits 0
4. cargo test -p speck-net fails with expected RED compile errors (GREEN after Plans 11-03 and 11-04)
</success_criteria>

## Artifacts this phase produces

The following symbols are newly created by Phase 11. The plan-review-convergence pass must exclude these from drift verification (they are new, not modifications to existing symbols).

Created in this plan (stubs only — no production code):
- crates/speck-net/tests/dns_split_test.rs — new test file

<output>
Create `.planning/phases/11-vpn-proof-dns/11-01-SUMMARY.md` when done
</output>
